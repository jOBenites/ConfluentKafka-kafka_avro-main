apiVersion: v1
kind: Service
metadata:
  name: order-api-service
  namespace: order-service
  labels:
    app: order-api
    component: api
spec:
  selector:
    app: order-api
  ports:
  - port: 80
    targetPort: 8181
    protocol: TCP
    name: http
  type: LoadBalancer

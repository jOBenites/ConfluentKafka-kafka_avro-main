apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-api
  namespace: order-service
  labels:
    app: order-api
    component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-api
  template:
    metadata:
      labels:
        app: order-api
        component: api
    spec:
      initContainers:
      - name: wait-for-rabbitmq
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z rabbitmq.order-service.svc.cluster.local 5672; do echo waiting for rabbitmq; sleep 2; done;']
      containers:
      - name: order-api
        image: docker.io/jodarb/order-service:latest
        ports:
        - containerPort: 8181
          protocol: TCP
        envFrom:
        - configMapRef:
            name: order-service-config
        - secretRef:
            name: order-service-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        startupProbe:
          httpGet:
            path: /q/health
            port: 8181
          failureThreshold: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /q/health/ready
            port: 8181
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /q/health/live
            port: 8181
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
      terminationGracePeriodSeconds: 30
